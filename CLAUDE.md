# 项目规则

- `127.0.0.1:7897` 只能作为可选 SOCKS5 前置代理：端口可用时先通过它连接用户输入的
  HTTP 供应商代理，不可用时直接连接供应商代理；不得把本地端口改成强制依赖。
- 本项目唯一写入、启动、重启、部署和验收范围是 `D:\Desktop\juanxing_GCash`；其他项目、端口或服务只允许只读读取与检查，禁止任何状态变更。
- 只读检查其他目标时不得调用会产生任务、网络副作用或持久化写入的接口。
